// Deliberately not "server-only" — both the server-side test-assistant-service.ts and the
// client-side "Try it out" page need this exact same literal, so it lives in its own file
// instead of being duplicated (and risking drift) in each.
export const TEST_PATIENT_PHONE = "+10000000001";
